# CLAUDE.md — このリポジトリでの恒久ルール

このリポジトリは OWASP WSTG (v4.2) の実施を支援する **方法論とツールの置き場** である。
実データ（生エビデンス）はここでは扱わない。

## 1. 絶対境界

- `evidence/**` は **読まない・書かない・要約しない**。存在しない前提で作業すること。
  - 同様に `docs/owasp/evidence/**`、`docs/owasp/**` 配下の `*.xlsx` 実査結果など、
    実案件のデータらしきものには一切アクセスしない。
- `docs/owasp/**`（WSTG 原文）は公開情報なので **読んでよい**。ただし
  **コミット・追跡しない**（`.gitignore` 済み。`scripts/fetch_wstg.sh` で再取得する）。
- 実データの分析・所見の作成依頼は **受けない**。担当はローカル手作業または社内 Gemini。
  依頼された場合は、その旨を伝えて方法論側（スクリプト・カード）の改善に誘導する。

## 2. 生成・編集してよい場所

- `scripts/`
- `matrix/`
- `playbooks/`
- `templates/`
- `README.md`

これ以外（特に `evidence/`）へは書き込まない。

## 3. 設計方針

- WSTG 項目を1つずつ潰すのではなく、**収集アクティビティ単位**で動き、
  1回の収集で複数の WSTG-ID に一括でチェックを入れる。
  対応表は `matrix/coverage.yaml`（`by_activity` / `by_wstg` の双方向インデックス）。
- チェックリスト本体（`checklist_export.csv`）は **概要のみ**。詳細は各エビデンス
  フォルダを見れば分かる、という前提で書く。
- `playbooks/WSTG-*.md` は **1テスト=1枚・自己完結・小サイズ**。社内 Gemini に
  データと一緒に貼れること、新人への説明台本に流用できることを満たす。
- 依存は標準ライブラリ中心。YAML は PyYAML まで。
